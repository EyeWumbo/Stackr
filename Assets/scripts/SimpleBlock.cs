using UnityEngine;
using System.Collections;

public class SimpleBlock : MonoBehaviour {

	bool isActive = true;
	bool isBackground = true;

	// Use this for initialization
	void Start () {
		Debug.Log ("Start then.");
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
